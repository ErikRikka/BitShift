# локальные настройки Claude Code
.claude/
*.local.json

# временные файлы сборки и тестов
*.tmp
*_probe.*
_preview.html
vt/
vtest*/

# результаты конвертации, если вдруг окажутся рядом
*_v2.mp4
*_v2.mov

# записка о состоянии сессии, живёт только локально
HANDOFF.md

# мак-версия: окружение и сборка
# в mac/build и mac/dist лежат скачанные ffmpeg и ffprobe по 48 МБ —
# GitHub блокирует файлы больше 100 МБ, а в истории они остались бы навсегда
mac/.venv/
mac/build/
mac/dist/
__pycache__/
*.py[cod]

# мусор macOS
.DS_Store
